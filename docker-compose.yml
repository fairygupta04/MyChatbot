services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}

  frontend:
    build: ./myChatbot
    ports:
      - "3000:3000"
    depends_on:
      - backend